import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFEF6C00);       // Deep Orange
  static const Color secondary = Color(0xFFFB8C00);     // Light Orange
  static const Color background = Color(0xFFF5F5F5);    // Soft gray
  static const Color textPrimary = Color(0xFF212121);   // Dark Text
  static const Color textSecondary = Color(0xFF757575); // Light Text
  static const Color accent = Color(0xFFFFC107);        // Amber
  static const Color white = Colors.white;
}
